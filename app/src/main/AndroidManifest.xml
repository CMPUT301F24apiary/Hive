<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hive">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/hivelogo"
        android:label="Hive"
        android:roundIcon="@mipmap/hivelogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hive"
        tools:targetApi="31">

        <!-- Set NotificationsActivity as the launcher activity temporarily -->
        <activity
            android:name=".NotificationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- EventListActivity without launcher intent for now -->
        <activity android:name=".EventListActivity" android:exported="true" />

        <!-- Keep MainActivity for navigation, but no launcher intent -->
        <activity android:name=".MainActivity" />

        <!-- Profile Activities -->
        <activity android:name=".ProfileActivity" />
        <activity android:name=".ProfileEditActivity" />

    </application>
</manifest>
